#include <stdio.h>
#include <string>
#include <cstring>

#pragma once

using namespace std;

namespace L
{
    class Lib
    {
        public:
            string spacer = "------------------------------\n";

            string titleCase(string S); // I wrote a function to titleCase characters
    };
}
